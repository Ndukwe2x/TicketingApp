/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['picsum.photos', 'images.unsplash.com', 'fastly.picsum.photos', 'example.com'],
    },
};

export default nextConfig;
